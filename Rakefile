begin
  require 'jeweler'
  Jeweler::Tasks.new do |gemspec|
    gemspec.name = "deactivated"
    gemspec.summary = "Replaces the delete process of a record."
    gemspec.description = "Replaces the deletion of records in the database with an a boolean that determines if the record has been 'deleted'."
    gemspec.email = "aaronaberg@gmail.com"
    gemspec.homepage = "http://github.com/aarona/deactivated"
    gemspec.authors = ["Aaron Aberg"]
  end
  Jeweler::GemcutterTasks.new
rescue LoadError
  puts "Jeweler not available. Install it with: sudo gem install jeweler -s http://gemcutter.org"
end